{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "composer",
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo 'Hello from composer project!'"
      }
    }
  }
}
